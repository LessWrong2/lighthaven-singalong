import Controller from "@/components/Controller";

// Host desk for the canonical session: search songs, run the queue, and drive
// the lyrics by hand (band mode) or on the song's own timing (auto mode).
export default function DashboardPage() {
  return <Controller />;
}
