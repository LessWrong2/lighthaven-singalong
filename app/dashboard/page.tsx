import Controller from "@/components/Controller";

// Host desk for the canonical session: search songs, run the queue, drive the
// lyrics (band mode) or play the recording (playback mode).
export default function DashboardPage() {
  return <Controller />;
}
