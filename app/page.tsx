import Display from "@/components/Display";

// The public lyrics screen: opens the one canonical session and shows the
// synced words. Put this page fullscreen on every display around the venue.
export default function LyricsScreenPage() {
  return <Display concert />;
}
